- name: install Nginx
  hosts: web
  become: yes 
  serial: 3
  tasks: 
   - name: install nginx
     ansible.builtin.package:
       name: nginx
       state: installed

   - name: start the service 
     ansible.builtin.service:
       name: nginx
       state: started
       enabled: yes