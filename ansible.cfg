[defaults]
inventory = inventory.aws_ec2.yml
remote_user = ec2-user
ask-vault-pass=true # you can view, encrypt, decrpt vault secret
forks =10 # acts for the task level  , 1st 10 servers it connects and then  do the next task for the same set of servers before moving to the next set of servers .